<PLAN>
    <OBJECTIVE>Address mobile UI deformations, analytics logic bugs, and React memory leaks on DashboardPage.jsx</OBJECTIVE>
    
    <STEPS>
        <STEP>
            <TARGET>frontend/src/pages/DashboardPage.jsx</TARGET>
            <ACTION>Update the repColor calculation to default to var(--text-muted) instead of var(--danger) when no reviews are present to eliminate the "Red Zero" bug.</ACTION>
        </STEP>
        <STEP>
            <TARGET>frontend/src/pages/DashboardPage.jsx</TARGET>
            <ACTION>Remove the local `toast` useState and setTimeout cleanup from `LiveEngineStats`. Pass down the global useToast instance from the parent DashboardPage to prevent unmounted component warning states.</ACTION>
        </STEP>
        <STEP>
            <TARGET>frontend/src/pages/DashboardPage.jsx</TARGET>
            <ACTION>Update flex containers in `LiveEngineStats` `<div className="flex gap-4">` to `<div className="flex flex-wrap gap-4">` to fix mobile word-crush bugs on 320px screens.</ACTION>
        </STEP>
        <STEP>
            <TARGET>frontend/src/pages/DashboardPage.jsx</TARGET>
            <ACTION>Refactor the grid layout for Auto-Reply Heartbeat in `DashboardInsights` from `className="grid-3 gap-6"` to `className="flex flex-wrap gap-6"` alongside flex bases to prevent excessive vertical stacking bloat on mobile.</ACTION>
        </STEP>
        <STEP>
            <TARGET>frontend/src/pages/DashboardPage.jsx</TARGET>
            <ACTION>Verify frontend responsive behaviors under 640px viewport.</ACTION>
        </STEP>
    </STEPS>
</PLAN>
